package class12;

public class E1StringClass {
    {
        //create the object of that class
        //1st we have to create the object
        //ClassName objectName= new ClassName();
        String string=new String("Ana");//Scanner scanner=new Scanner(System.in);//ex
        //ex;Dog dog=new Dog();
        String string1="Ana";//Most widely used one
        System.out.println(string1.length());//there are 3 letters in name Ana(3)
        String name="Justin Chukwudiche";
        if (name.length()>8){
            System.out.println("Only 8 characters are allowed");
        }
    }
}
