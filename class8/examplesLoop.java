package class8;

public class examplesLoop {
    public static void main(String[] args) {
        //write a loop to add all the even num from 1 to 10
int sum=0;//creating a var to hold the sum
    for(int i=1;i<=10;i++){
        if(i%2==0){//checking if a num is even
            sum=sum+i;//sum+=;// (the same)//add even num to the sum vari
        }

        }
        System.out.println(sum);
       }
    }

